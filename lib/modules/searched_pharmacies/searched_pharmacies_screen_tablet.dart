import 'package:flutter/material.dart';
import 'package:salesrep/models/user_pharmacies_model.dart';

class SearchedPharmaciesScreenTablet extends StatelessWidget {
  List<PharmacyData>? searchedList;
  SearchedPharmaciesScreenTablet({this.searchedList});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
