import 'package:flutter/material.dart';

class HistoryScreenTablet extends StatelessWidget {
  const HistoryScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('History'),
    );
  }
}
