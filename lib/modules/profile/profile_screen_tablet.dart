import 'package:flutter/material.dart';

class ProfileScreenTablet extends StatelessWidget {
  const ProfileScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Profile'),
    );
  }
}
